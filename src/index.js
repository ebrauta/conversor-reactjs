import React from 'react';
import ReactDOM from 'react-dom';

const rootElement = document.getElementById('root')

const Index = () => {
    return <h1>Index</h1>
}

ReactDOM.render(
    <Index />,
    rootElement
);

